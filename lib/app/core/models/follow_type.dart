/// Enum representing the type of follow list to display
enum FollowType {
  /// List of users who are following the selected user
  followers,
 
  /// List of users that the selected user is following
  following
} 